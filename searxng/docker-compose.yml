version: '3'
services:
  app:
    image: 'searxng/searxng'
    restart: unless-stopped
    environment:
      - 'BASE_URL=https://searx.home.arpa'
      - 'INSTANCE_NAME=1'
    volumes:
      - "${BASEPATH}/${COMPOSE_PROJECT_NAME}:/etc/searxng:z"
    ports:
      - "8080:8080"
