services:
  app:
    image: ghcr.io/kimdre/doco-cd:0.110.1
    restart: always
    environment:
      TZ: Europe/Brussels
      SCHEDULER_ENABLED: false ## Disable on redeploy stack to avoid duplicate schedule jobs https://doco.cd/latest/Advanced/Job-Scheduling/
      LOG_LEVEL: debug
      PASS_ENV: "true"
      APPRISE_API_URL: http://apprise:8000/notify
      APPRISE_NOTIFY_LEVEL: info
      APPRISE_NOTIFY_URLS_FILE: /run/secrets/apprise_urls.txt
      GIT_AUTH_DOMAINS_FILE: /run/secrets/auth_domains.txt
      SOPS_AGE_KEY_FILE: /run/secrets/keys.txt
      POLL_CONFIG: |
        - url: "${MY_REPO_URL}"
          interval: 960
          reference: xenon
          deployments:
            - name: "doco-cd"
              working_dir: "doco-cd/"
              compose_files:
                - docker-compose.yml
              timeout: 300
              environment:
                BASEPATH: /data
                TZ: Europe/Brussels
    volumes:
      - "${BASEPATH}/${COMPOSE_PROJECT_NAME}/data:/data"
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "/root/.config/sops/age/keys.txt:/run/secrets/keys.txt:ro"
      - "./auth_domains.txt:/run/secrets/auth_domains.txt:ro"
      - "./apprise_urls.txt:/run/secrets/apprise_urls.txt:ro"
    healthcheck:
      test: ["CMD", "/doco-cd", "healthcheck"]
      start_period: 15s
      interval: 30s
      timeout: 5s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    depends_on:
      apprise:
        condition: service_healthy

  apprise:
    image: caronc/apprise:v1.5.1
    restart: always
    environment:
      TZ: Europe/Brussels
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8000/status >/dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "2"
